import math
import cmath
b=10**(-20)
c=3.0
def solve(b,c):
  A=2.0*c/b**2
  D=b*b-4*c
  if D>=0:
    D=math.sqrt(b*b-4*c)
  else:
    D=cmath.sqrt(b*b-4*c)
  if A  < 10**-15:
      x1=-c/b
      x2=c/x1 
  else:
      x1=(-b+D)/2
      x2=c/x1
  return(x1,x2)
print (solve(b,c))
